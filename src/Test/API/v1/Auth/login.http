POST {{BASE_URL}}/users/login
Content-Type: application/json

{
  "user": {
    "username": "johndoe",
    "password": "securepassword"
  }
}

{%

  local body = context.json_decode(context.result.body)

  context.set_env("ACCESS_TOKEN", body.user.token)

%}
